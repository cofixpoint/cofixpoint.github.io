{
  "name": "cofixpoint.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "oxlint --no-error-on-unmatched-pattern .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check ."
  },
  "devDependencies": {
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0",
    "vite": "^8.1.3"
  },
  "engines": {
    "node": ">=22.14.0"
  }
}
